import React from "react";
import { Grid } from "@material-ui/core";

export const Layout = ({ children }) => {
	// Header
	// View
	// Footer
	// Sidebar
	console.log(children);
	return (
		<>
			<Grid container>
				<Grid item>SL Cohort Tracker</Grid>
			</Grid>
		</>
	);
};

// export default Layout
